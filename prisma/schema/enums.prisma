enum Role {
  SUPER_ADMIN
  AGENCY_ADMIN
  AGENCY_STAFF
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

/// Drives the read-only gate: a TRIAL agency past trialEndsAt, or an EXPIRED /
/// SUSPENDED one, may still GET but is blocked from any write.
enum AgencyStatus {
  TRIAL
  ACTIVE
  EXPIRED
  SUSPENDED
}

/// What a plan unlocks. Checked by checkFeatureAccess(); every feature is
/// unlocked while an agency is still inside its trial window.
enum PlanFeature {
  TICKETING
  VISA
  HAJJ_UMRAH
  TOURS
  HOTEL
  EXPENSE
  REPORTS
  CRM
}

/// One-way state machine, enforced in the service layer:
/// ISSUED -> REISSUED | REFUNDED | VOID ; REISSUED -> REFUNDED | VOID.
/// REFUNDED and VOID are final.
enum TicketStatus {
  ISSUED
  REISSUED
  REFUNDED
  VOID
}

/// One-way: SUBMITTED -> PROCESSING -> APPROVED | REJECTED ; APPROVED -> DELIVERED.
/// REJECTED and DELIVERED are final.
enum VisaStatus {
  SUBMITTED
  PROCESSING
  APPROVED
  REJECTED
  DELIVERED
}

enum HajjBookingStatus {
  RESERVED
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum HajjPackageType {
  HAJJ
  UMRAH
}

enum DocumentStatus {
  PENDING
  RECEIVED
  VERIFIED
}

enum PaymentMethod {
  CASH
  BANK_TRANSFER
  CARD
  MOBILE_BANKING
  CHEQUE
  OTHER
}

enum SubscriptionOrderStatus {
  PENDING
  SUCCESS
  FAILED
  CANCELLED
}

enum HajjTier {
  ECONOMY
  PREMIUM
  VIP
}

enum HajjMealPlan {
  NONE
  BREAKFAST
  FULL_BOARD
}

enum HajjBatchStatus {
  OPEN
  FULL
  DEPARTED
  COMPLETED
  CANCELLED
}

enum HajjHotelType {
  MAKKAH
  MADINAH
}

/// A tour stops taking bookings when it closes, and a cancelled tour frees
/// every seat on it. COMPLETED is the trip having run.
enum TourPackageStatus {
  OPEN
  CLOSED
  COMPLETED
  CANCELLED
}

/// One-way, enforced in the service layer:
/// RESERVED -> CONFIRMED | CANCELLED ; CONFIRMED -> COMPLETED | CANCELLED.
/// COMPLETED and CANCELLED are final.
enum TourBookingStatus {
  RESERVED
  CONFIRMED
  COMPLETED
  CANCELLED
}

/// The same one-way lifecycle a tour booking has, for the same reasons:
/// COMPLETED is the stay having happened, CANCELLED bills nothing.
enum HotelBookingStatus {
  RESERVED
  CONFIRMED
  COMPLETED
  CANCELLED
}

/// What an import run was carrying. The master lists have to exist before
/// the history that points at them, so a run is one stage or the other — or
/// EVERYTHING, which is the two back to back and what the screen actually
/// offers, because an agency moving in wants one button.
enum ImportStage {
  FOUNDATIONS
  HISTORY
  EVERYTHING
}

/// A run takes minutes on a real workbook, so it is not over when the request
/// is: RUNNING is the normal state for a while, and the screen watches for it
/// to change.
enum ImportStatus {
  RUNNING
  COMPLETED
  FAILED
  REVERTED
}

/// Which side of the ledger a posting sits on. Every money movement in the
/// system is exactly one AccountPosting with one of these directions.
enum PostingDirection {
  IN
  OUT
}

/// What caused a posting. Mirrors the columns of the source spreadsheet's
/// Balance Dashboard, so a per-source sum reproduces that sheet exactly.
enum PostingSource {
  OPENING
  INVESTMENT
  INVESTMENT_WITHDRAWAL
  PROFIT_WITHDRAWAL
  SUPPLIER_PAYMENT
  SALES_PAYMENT
  DATE_CHANGE_FEE
  DUE_RECEIVED
  EXPENSE
  EMPLOYEE_PAYOUT
  TRANSFER_IN
  TRANSFER_OUT
  ADJUSTMENT
}

enum CashAccountCategory {
  OWNER_FUNDS
  LOANS
  SALES_BUYING
  OTHERS
}

/// Owner capital moving in or out of the business.
enum CapitalFlowType {
  INVEST
  WITHDRAW
}

enum AttendanceStatus {
  PRESENT
  ABSENT
}

/// Only payouts — there is no advance or deduction type.
enum EmployeePayoutType {
  SALARY
  BONUS
}

enum AnnouncementType {
  INFO
  FEATURE
  MAINTENANCE
  WARNING
}

enum SupportCategory {
  BILLING
  TECHNICAL
  FEATURE_REQUEST
  OTHER
}

enum SupportPriority {
  LOW
  MEDIUM
  HIGH
}

enum SupportStatus {
  OPEN
  IN_PROGRESS
  RESOLVED
  CLOSED
}

enum PlanHistoryAction {
  ASSIGNED
  UPGRADED
  DOWNGRADED
  TRIAL_EXTENDED
}

/// Which subscription email an agency was sent. One of each per period end.
enum AgencyReminderKind {
  TRIAL_ENDING_3_DAYS
  TRIAL_ENDING_1_DAY
  SUBSCRIPTION_ENDING_7_DAYS
  SUBSCRIPTION_ENDING_1_DAY
  EXPIRED
}
